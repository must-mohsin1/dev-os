# dev-os :: devos
# A Hermes profile distribution. Install with:  hermes profile install ./agents/devos --yes
name: devos
version: 0.1.1
description: "high|Dev OS coordinator — decomposes goals, routes to researcher/planner/devcrew, gates at the plan, tracks the board, reports."
hermes_requires: ">=0.12.0"
author: "dev-os"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "OpenRouter API key — fallback inference (primary is OAuth: Codex / xAI-Grok)"
    required: false
distribution_owned:
  - SOUL.md
  - config.yaml
  - skills
