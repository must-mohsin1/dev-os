# dev-os :: planner
# A Hermes profile distribution. Install with:  hermes profile install ./agents/planner --yes
name: devos-planner
version: 0.1.0
description: "high|Planning specialist — turns goal + research into approval-ready specs with acceptance criteria; self-critiques."
hermes_requires: ">=0.12.0"
author: "dev-os"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "OpenRouter API key — fallback inference (primary is OAuth: Codex / xAI-Grok)"
    required: false
distribution_owned:
  - SOUL.md
  - config.yaml
  - skills
